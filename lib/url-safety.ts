export type PublicUrlIssue="invalid-url"|"not-https"|"embedded-credentials"|"private-host"|"secret-query-parameter";
const highConfidenceSecretParams=new Set(["api_key","apikey","access_token","client_secret","secret_key","private_key","password","passwd","authorization"]);
function isPrivateHost(hostname:string){const h=hostname.toLowerCase().replace(/^\[|\]$/g,"");if(h==="localhost"||h.endsWith(".localhost")||h.endsWith(".local")||h.endsWith(".internal")||h==="::1"||h==="0.0.0.0")return true;if(/^127\./.test(h)||/^10\./.test(h)||/^192\.168\./.test(h)||/^169\.254\./.test(h))return true;const m=h.match(/^172\.(\d{1,3})\./);return Boolean(m&&Number(m[1])>=16&&Number(m[1])<=31)}
export function inspectPublicUrl(value:string):PublicUrlIssue[]{let url:URL;try{url=new URL(value)}catch{return ["invalid-url"]}const issues:PublicUrlIssue[]=[];if(url.protocol!=="https:")issues.push("not-https");if(url.username||url.password)issues.push("embedded-credentials");if(isPrivateHost(url.hostname))issues.push("private-host");for(const key of url.searchParams.keys())if(highConfidenceSecretParams.has(key.toLowerCase())){issues.push("secret-query-parameter");break}return issues}
