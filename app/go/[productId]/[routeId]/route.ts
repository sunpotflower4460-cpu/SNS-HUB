import {byId,healthyRoutes} from "@/lib/hub";export async function GET(_r:Request,{params}:{params:Promise<{productId:string;routeId:string}>}){const {productId,routeId}=await params,p=byId(productId);const route=p&&healthyRoutes(p).find(x=>x.routeId===routeId);if(!route)return new Response("Route unavailable",{status:404});console.info("hub_event",JSON.stringify({type:"route_click",productId,routeId,receivedAt:new Date().toISOString()}));return Response.redirect(route.url,302)}
