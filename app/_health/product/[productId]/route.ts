import {byId,manifest} from "@/lib/hub";export const dynamic="force-dynamic";export async function GET(_r:Request,{params}:{params:Promise<{productId:string}>}){const {productId}=await params,p=byId(productId);if(!p)return Response.json({ok:false,productId},{status:404});return Response.json({ok:["ready","published","archived"].includes(p.publication.status),productId,publicUrl:`/p/${p.slug}`,contentVersion:manifest().contentVersion,publicationStatus:p.publication.status})}
