import {byId,isPublishReadyProduct,isPublicProduct,manifest} from "@/lib/hub";
export const dynamic="force-dynamic";
export async function GET(request:Request,{params}:{params:Promise<{productId:string}>}){const {productId}=await params,p=byId(productId),headers={"Cache-Control":"no-store"};if(!p)return Response.json({ok:false,productId},{status:404,headers});const publiclyReachable=isPublicProduct(p),publishReady=isPublishReadyProduct(p);return Response.json({ok:publishReady,publiclyReachable,publishReady,featured:p.publication.status==="published",productId,publicUrl:new URL(`/p/${p.slug}`,request.url).toString(),contentVersion:manifest().contentVersion,publicationStatus:p.publication.status,availability:p.availability},{headers})}
